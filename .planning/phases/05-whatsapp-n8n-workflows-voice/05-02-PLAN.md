---
phase: 05-whatsapp-n8n-workflows-voice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - N8N-01
  - N8N-02
  - N8N-03
  - N8N-04
  - N8N-05
  - N8N-06
  - VOIC-03
must_haves:
  truths:
    - "Boas vindas workflow resolves expert_id from instance and filters leads by expert_id"
    - "Follow up workflow processes only leads belonging to the expert that owns the triggering instance"
    - "Envio Mensagem workflow validates expert_id before sending and uses expert's UAZAPI token"
    - "All 3 workflows read nome_assistente from expert record and substitute Helena references"
    - "All 3 workflows read configuracoes filtered by expert_id"
    - "Audio-generating nodes use expert's voice_id and voice_settings from Minimax"
  artifacts:
    - path: "n8n-workflow:fGIHZHMvy3NdJzDQ"
      provides: "Boas vindas - Leads Insta with expert_id filtering"
    - path: "n8n-workflow:Ukax93riMgu0ZuKt"
      provides: "Follow up - assistente with expert_id filtering"
    - path: "n8n-workflow:7WKSEmy5qfjb2MKu"
      provides: "Envio Mensagem - Saas with expert_id validation"
  key_links:
    - from: "Webhook trigger node"
      to: "Supabase lookup node"
      via: "Instance name from webhook → whatsapp_rotacao query → expert_id"
      pattern: "whatsapp_rotacao WHERE instancia"
    - from: "Expert lookup node"
      to: "Supabase query nodes"
      via: "expert_id injected into WHERE clause of all subsequent queries"
      pattern: "WHERE expert_id"
    - from: "Expert lookup node"
      to: "Minimax TTS node"
      via: "voice_id and voice_settings from expert record"
      pattern: "voice_id"
---

<objective>
Update the 3 message-oriented n8n workflows (Boas vindas, Follow up, Envio Mensagem) to be multi-tenant: resolve expert_id from instance, filter all Supabase queries by expert_id, use expert's UAZAPI tokens, substitute nome_assistente for "Helena", and use expert's voice_id/voice_settings for audio generation.

Purpose: These are the core messaging workflows. Without expert_id filtering, a new expert's leads would be mixed with Allan's data in workflow processing.

Output: 3 n8n workflows updated in-place with expert_id resolution, filtering, token routing, nome_assistente substitution, and voice settings.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-whatsapp-n8n-workflows-voice/05-CONTEXT.md

<interfaces>
<!-- N8N workflow IDs to update -->
Workflow 1: "Boas vindas - Leads Insta" (fGIHZHMvy3NdJzDQ)
Workflow 2: "Follow up - assistente" (Ukax93riMgu0ZuKt)
Workflow 3: "Envio Mensagem - Saas" (7WKSEmy5qfjb2MKu)

<!-- Supabase tables involved -->
- whatsapp_rotacao: has expert_id, instancia, token, nome, numero columns
- experts: has id, nome_assistente, voice_id, voice_settings columns
- configuracoes: has expert_id, chave, valor columns
- leads: has expert_id column, filtered in all queries

<!-- Existing RPCs -->
- validate_webhook_expert(p_expert_id UUID, p_instancia TEXT) -> boolean
  Validates that the instance belongs to the claimed expert_id

<!-- Minimax TTS API -->
POST https://api.minimax.io/v1/t2a_async_v2
Model: speech-2.8-hd
Headers: Authorization: Bearer {MINIMAX_API_KEY}
Body: { model: "speech-2.8-hd", text: "...", voice_id: "{expert.voice_id}", voice_setting: { speed: N, pitch: N, vol: N, timbre: N } }

<!-- Decision references -->
D-04: N8N does LOOKUP in whatsapp_rotacao to find expert_id from instance name
D-06: Update workflows IN-PLACE (safe with 1 expert)
D-08: Each workflow adds: lookup expert_id → filter queries → use expert tokens → use voice
D-11: Audio in n8n workflows with expert's voice_id
D-15: "Helena" only in n8n workflows — substitute with nome_assistente
D-16: Workflows read nome_assistente from expert record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect and update Boas vindas + Follow up workflows</name>
  <files>n8n-workflow:fGIHZHMvy3NdJzDQ, n8n-workflow:Ukax93riMgu0ZuKt</files>
  <read_first>
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow fGIHZHMvy3NdJzDQ (Boas vindas)
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow Ukax93riMgu0ZuKt (Follow up)
    - Understand the current node structure, Supabase query nodes, HTTP Request nodes, and any existing filtering
  </read_first>
  <action>
**For EACH of the 2 workflows, apply the same multi-tenant pattern (per D-08):**

**Step A: Add Expert Resolution Node (after webhook trigger or schedule trigger)**
Add a Supabase node (or Code node with HTTP Request) that:
1. Takes the instance name/number from the trigger payload
2. Queries: `SELECT wr.expert_id, e.nome_assistente, e.voice_id, e.voice_settings, wr.token FROM whatsapp_rotacao wr JOIN experts e ON e.id = wr.expert_id WHERE wr.instancia = '{{instance_name}}' AND wr.ativo = true LIMIT 1`
3. Stores the result as `expert_id`, `nome_assistente`, `voice_id`, `voice_settings`, `uazapi_token` for downstream nodes

**Step B: Filter all Supabase query nodes by expert_id**
For every Supabase node that queries leads, mensagens, mensagens_funil_v2, or any other table:
- Add `AND expert_id = '{{expert_id}}'` to the WHERE clause
- This ensures the workflow only processes data belonging to this expert

**Step C: Use expert's UAZAPI token (per N8N-03)**
For HTTP Request nodes that call UAZAPI API:
- Replace any hardcoded token with `{{uazapi_token}}` from the expert resolution node
- The token comes from whatsapp_rotacao.token for the resolved instance

**Step D: Query configuracoes by expert_id (per N8N-02)**
For any node that reads from configuracoes table:
- Add `WHERE expert_id = '{{expert_id}}'` to the query
- Config keys: link_comunidade, link_cadastro, horario_lives, followup_*_horas, pergunta_entrada_horas (per D-10)

**Step E: Substitute nome_assistente for "Helena" (per D-15, D-16)**
Find all nodes where the text "Helena" appears (in message templates, text formatting, etc.):
- Replace hardcoded "Helena" with `{{nome_assistente}}` from the expert resolution node
- Check Code nodes, Set nodes, and any text-producing nodes

**Step F: Use expert's voice_id in Minimax calls (per VOIC-03, D-11)**
For HTTP Request nodes that call Minimax TTS API:
- Replace hardcoded voice_id with `{{voice_id}}` from expert resolution
- Replace hardcoded voice_settings with the expert's voice_settings JSON
- If voice_id is null/empty, skip the audio generation step (add an IF node to check)

**IMPORTANT per D-06:** Update in-place. Do not clone workflows. Safe because only 1 expert exists currently.

Use MCP tools:
- `mcp__claude_ai_n8n__get_workflow_details` to inspect current structure
- `mcp__claude_ai_n8n__update_workflow` to save changes (if available)
- If update tool is not available, document the exact JSON changes needed
  </action>
  <verify>
    <automated>MISSING — Use mcp__claude_ai_n8n__get_workflow_details to verify the updated workflows contain expert_id resolution nodes and filtered queries</automated>
  </verify>
  <acceptance_criteria>
    - Boas vindas workflow (fGIHZHMvy3NdJzDQ) has expert resolution node after trigger
    - Boas vindas workflow filters leads queries by expert_id
    - Boas vindas workflow uses expert's UAZAPI token for sending
    - Follow up workflow (Ukax93riMgu0ZuKt) has expert resolution node
    - Follow up workflow processes only expert's leads
    - Both workflows substitute "Helena" with nome_assistente
    - Both workflows use expert's voice_id for audio (if applicable)
    - Both workflows query configuracoes with expert_id filter
  </acceptance_criteria>
  <done>Both Boas vindas and Follow up workflows resolve expert_id from instance, filter all queries by expert_id, use expert's tokens, substitute nome_assistente, and use expert voice settings.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect and update Envio Mensagem - Saas workflow</name>
  <files>n8n-workflow:7WKSEmy5qfjb2MKu</files>
  <read_first>
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow 7WKSEmy5qfjb2MKu (Envio Mensagem)
    - Understand the current node structure -- this is the message-sending workflow called from the frontend
    - Note: this workflow receives expert_id in the webhook payload (from Plan 01 Task 1)
  </read_first>
  <action>
**This workflow is called FROM the frontend (via WEBHOOKS.ENVIO_SAAS). Plan 01 enriches the webhook payload with expert_id. This workflow must validate and use that expert_id.**

**Step A: Validate expert_id from payload (per N8N-06, AUTH-12)**
Add a validation step after the webhook trigger:
1. Extract expert_id from the incoming webhook payload
2. Call validate_webhook_expert RPC: `SELECT validate_webhook_expert('{{expert_id}}', '{{instancia}}')` to confirm the instance belongs to the expert
3. If validation fails, return error response and stop workflow

**Step B: Expert resolution (same pattern as Task 1)**
Query experts table to get nome_assistente, voice_id, voice_settings:
`SELECT nome_assistente, voice_id, voice_settings FROM experts WHERE id = '{{expert_id}}'`

**Step C: Filter all Supabase queries by expert_id**
Same as Task 1 Step B — add expert_id filter to all table queries

**Step D: Use expert's UAZAPI token**
Query whatsapp_rotacao for the expert's active instance token:
`SELECT token, instancia FROM whatsapp_rotacao WHERE expert_id = '{{expert_id}}' AND ativo = true ORDER BY ordem LIMIT 1`
Use this token in UAZAPI HTTP Request nodes

**Step E: Configuracoes per-expert**
Same as Task 1 Step D

**Step F: Nome_assistente substitution**
Same as Task 1 Step E

**Step G: Voice_id for audio**
Same as Task 1 Step F

Use same MCP tools as Task 1 for inspection and update.
  </action>
  <verify>
    <automated>MISSING — Use mcp__claude_ai_n8n__get_workflow_details to verify the updated workflow contains expert_id validation, resolution, and filtered queries</automated>
  </verify>
  <acceptance_criteria>
    - Envio Mensagem workflow (7WKSEmy5qfjb2MKu) validates expert_id from webhook payload
    - Uses validate_webhook_expert RPC to confirm instance ownership
    - All Supabase queries filtered by expert_id
    - Uses expert's UAZAPI token (not hardcoded global token)
    - Substitutes "Helena" with nome_assistente
    - Uses expert's voice_id for audio generation
    - Returns error if expert_id validation fails
  </acceptance_criteria>
  <done>Envio Mensagem workflow validates expert_id, filters all queries, uses expert tokens, substitutes nome_assistente, and uses expert voice settings.</done>
</task>

</tasks>

<verification>
1. Get workflow details for all 3 workflows and confirm expert_id resolution nodes exist
2. Confirm all Supabase query nodes include expert_id in WHERE clause
3. Confirm UAZAPI HTTP Request nodes use dynamic token from expert resolution
4. Confirm "Helena" string is replaced with nome_assistente reference in all text nodes
5. Confirm Minimax TTS nodes use dynamic voice_id from expert record
6. Confirm Envio Mensagem validates expert_id via RPC before processing
</verification>

<success_criteria>
- 3 workflows (Boas vindas, Follow up, Envio Mensagem) are multi-tenant aware
- Expert_id resolved from instance for trigger-based workflows
- Expert_id validated from payload for frontend-triggered workflows
- All queries, tokens, names, and voice settings are expert-scoped
</success_criteria>

<output>
After completion, create `.planning/phases/05-whatsapp-n8n-workflows-voice/05-02-SUMMARY.md`
</output>
