---
phase: 05-whatsapp-n8n-workflows-voice
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/phases/05-whatsapp-n8n-workflows-voice/N8N_MIGRATION_GUIDE.md
  - supabase/migrations/20260328_05_02_followup_expert_rpc.sql
autonomous: true
requirements:
  - N8N-01
  - N8N-02
  - N8N-03
  - N8N-04
  - N8N-05
  - N8N-06
  - VOIC-03
must_haves:
  truths:
    - "Boas vindas workflow resolves expert_id from instance and filters leads by expert_id"
    - "Follow up workflow loops over all active experts and processes each expert's leads independently (schedule-triggered, no instance)"
    - "Envio Mensagem workflow validates expert_id before sending and uses expert's UAZAPI token"
    - "All 3 workflows read nome_assistente from expert record and substitute Helena references"
    - "All 3 workflows read configuracoes filtered by expert_id"
    - "Audio-generating nodes use expert's voice_id and voice_settings from Minimax"
  artifacts:
    - path: "n8n-workflow:fGIHZHMvy3NdJzDQ"
      provides: "Boas vindas - Leads Insta with expert_id filtering"
    - path: "n8n-workflow:Ukax93riMgu0ZuKt"
      provides: "Follow up - assistente with expert_id filtering"
    - path: "n8n-workflow:7WKSEmy5qfjb2MKu"
      provides: "Envio Mensagem - Saas with expert_id validation"
  key_links:
    - from: "Webhook trigger node"
      to: "Supabase lookup node"
      via: "Instance name from webhook → whatsapp_rotacao query → expert_id"
      pattern: "whatsapp_rotacao WHERE instancia"
    - from: "Expert lookup node"
      to: "Supabase query nodes"
      via: "expert_id injected into WHERE clause of all subsequent queries"
      pattern: "WHERE expert_id"
    - from: "Expert lookup node"
      to: "Minimax TTS node"
      via: "voice_id and voice_settings from expert record"
      pattern: "voice_id"
---

<objective>
Update the 3 message-oriented n8n workflows (Boas vindas, Follow up, Envio Mensagem) to be multi-tenant: resolve expert_id from instance, filter all Supabase queries by expert_id, use expert's UAZAPI tokens, substitute nome_assistente for "Helena", and use expert's voice_id/voice_settings for audio generation.

Purpose: These are the core messaging workflows. Without expert_id filtering, a new expert's leads would be mixed with Allan's data in workflow processing.

Dependency on 05-01: The Envio Mensagem workflow (7WKSEmy5qfjb2MKu) receives expert_id in the webhook payload from the frontend. Plan 05-01 enriches those payloads with expert_id. This plan must run after 05-01 so the webhook payloads are ready.

Output: 3 n8n workflows updated in-place with expert_id resolution, filtering, token routing, nome_assistente substitution, and voice settings.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-whatsapp-n8n-workflows-voice/05-CONTEXT.md

<interfaces>
<!-- N8N workflow IDs to update -->
Workflow 1: "Boas vindas - Leads Insta" (fGIHZHMvy3NdJzDQ)
Workflow 2: "Follow up - assistente" (Ukax93riMgu0ZuKt)
Workflow 3: "Envio Mensagem - Saas" (7WKSEmy5qfjb2MKu)

<!-- Supabase tables involved -->
- whatsapp_rotacao: has expert_id, instancia, token, nome, numero columns
- experts: has id, nome_assistente, voice_id, voice_settings columns
- configuracoes: has expert_id, chave, valor columns
- leads: has expert_id column, filtered in all queries

<!-- Existing RPCs -->
- validate_webhook_expert(p_expert_id UUID, p_instancia TEXT) -> boolean
  Validates that the instance belongs to the claimed expert_id

<!-- Minimax TTS API -->
POST https://api.minimax.io/v1/t2a_async_v2
Model: speech-2.8-hd
Headers: Authorization: Bearer {MINIMAX_API_KEY}
Body: { model: "speech-2.8-hd", text: "...", voice_id: "{expert.voice_id}", voice_setting: { speed: N, pitch: N, vol: N, timbre: N } }

<!-- Decision references -->
D-04: N8N does LOOKUP in whatsapp_rotacao to find expert_id from instance name
D-06: Update workflows IN-PLACE (safe with 1 expert)
D-08: Each workflow adds: lookup expert_id → filter queries → use expert tokens → use voice
D-11: Audio in n8n workflows with expert's voice_id
D-15: "Helena" only in n8n workflows — substitute with nome_assistente
D-16: Workflows read nome_assistente from expert record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Follow up expert RPC + Inspect all 3 workflows via MCP</name>
  <files>supabase/migrations/20260328_05_02_followup_expert_rpc.sql</files>
  <read_first>
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow fGIHZHMvy3NdJzDQ (Boas vindas)
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow Ukax93riMgu0ZuKt (Follow up)
    - Use mcp__claude_ai_n8n__get_workflow_details for workflow 7WKSEmy5qfjb2MKu (Envio Mensagem)
  </read_first>
  <action>
**Step A: Create Supabase RPC for Follow up per-expert (CRITICAL CORRECTION)**

The Follow up workflow (Ukax93riMgu0ZuKt) is SCHEDULE-TRIGGERED (every 15 min), NOT webhook-triggered. It has NO instance in the payload. The workflow must loop over all active experts.

Create and apply migration via mcp__claude_ai_Supabase__apply_migration (project_id: albdkqpvoyfhziozgwlk):

```sql
-- RPC for Follow up workflow: get leads needing followup for a specific expert
CREATE OR REPLACE FUNCTION buscar_leads_followup_expert(p_expert_id UUID)
RETURNS TABLE (
  lead_id UUID,
  telefone TEXT,
  nome TEXT,
  status TEXT,
  followup_enviado TIMESTAMPTZ,
  followup_convite_enviado TIMESTAMPTZ,
  followup_aguardando_cadastro TIMESTAMPTZ,
  followup_saiu_grupo TIMESTAMPTZ,
  followup_boas_vindas_grupo TIMESTAMPTZ,
  data_primeiro_contato TIMESTAMPTZ,
  ultima_interacao TIMESTAMPTZ,
  instancia_enviou TEXT,
  token_enviou TEXT,
  telefone_enviou TEXT
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  RETURN QUERY
  SELECT l.id, l.telefone, l.nome, l.status,
    l.followup_enviado, l.followup_convite_enviado,
    l.followup_aguardando_cadastro, l.followup_saiu_grupo,
    l.followup_boas_vindas_grupo, l.data_primeiro_contato,
    l.ultima_interacao, l.instancia_enviou, l.token_enviou, l.telefone_enviou
  FROM leads l
  WHERE l.expert_id = p_expert_id
  AND l.status IN ('primeiro_audio_enviado', 'convite_enviado', 'aguardando_cadastro', 'no_grupo', 'saiu_grupo')
  AND l.data_primeiro_contato IS NOT NULL;
END;
$$;
```

Save SQL to supabase/migrations/20260328_05_02_followup_expert_rpc.sql before applying.

**Step B: Inspect all 3 workflows and document current structure**

Use mcp__claude_ai_n8n__get_workflow_details for each workflow. Document:
- Trigger type (webhook vs schedule)
- All Supabase query nodes and their SQL
- All HTTP Request nodes (UAZAPI calls, Minimax calls)
- Where "Helena" appears in text
- Where tokens are hardcoded
- Current node connections/flow

This inspection feeds into Task 2 (the migration guide).
  </action>
  <verify>
    <automated>mcp__claude_ai_Supabase__execute_sql with query "SELECT proname FROM pg_proc WHERE proname = 'buscar_leads_followup_expert'" — must return 1 row. Also verify: mcp__claude_ai_n8n__get_workflow_details for all 3 workflows returns valid JSON.</automated>
  </verify>
  <acceptance_criteria>
    - buscar_leads_followup_expert RPC exists in Supabase and accepts p_expert_id UUID
    - All 3 workflows inspected and current structure documented
    - Migration SQL saved to supabase/migrations/
  </acceptance_criteria>
  <done>Follow up expert RPC created. All 3 message workflows inspected and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Generate N8N Migration Guide for all 3 message workflows</name>
  <files>.planning/phases/05-whatsapp-n8n-workflows-voice/N8N_MIGRATION_GUIDE.md</files>
  <read_first>
    - Task 1 inspection results (workflow structures)
    - .planning/phases/05-whatsapp-n8n-workflows-voice/05-CONTEXT.md (decisions D-04 through D-16)
  </read_first>
  <action>
**IMPORTANT: There is NO MCP tool to edit n8n workflows. The n8n MCP only supports: get_workflow_details, execute_workflow, get_execution, search_workflows, publish/unpublish. No update/edit capability.**

Generate a comprehensive markdown guide at `.planning/phases/05-whatsapp-n8n-workflows-voice/N8N_MIGRATION_GUIDE.md` with step-by-step instructions for manually editing each workflow in the n8n editor.

The guide must cover ALL 3 workflows with specific instructions:

**Workflow 1: Boas vindas - Leads Insta (fGIHZHMvy3NdJzDQ)**
- Trigger: Webhook (receives instance name)
- Add: Supabase node after trigger to resolve expert_id from instance name
  - SQL: `SELECT wr.expert_id, e.nome_assistente, e.voice_id, e.voice_settings, wr.token FROM whatsapp_rotacao wr JOIN experts e ON e.id = wr.expert_id WHERE wr.instancia = '{{$json.instancia}}' AND wr.ativo = true LIMIT 1`
- Modify: All Supabase query nodes — add `AND expert_id = '{{expert_id}}'`
- Modify: UAZAPI HTTP Request nodes — use `{{token}}` from expert resolution
- Modify: Replace "Helena" with `{{nome_assistente}}`
- Modify: Minimax TTS nodes — use `{{voice_id}}` and `{{voice_settings}}`
- Add: IF node before audio generation — skip if voice_id is empty

**Workflow 2: Follow up - assistente (Ukax93riMgu0ZuKt)**
- Trigger: Schedule (every 15 min) — NO INSTANCE IN PAYLOAD
- DIFFERENT PATTERN: Must loop over all active experts
- Add: Supabase node after Schedule trigger:
  - SQL: `SELECT id, nome_assistente, voice_id, voice_settings FROM experts WHERE ativo = true`
- Add: SplitInBatches node to loop over each expert
- Replace: Existing leads query with RPC call:
  - `SELECT * FROM buscar_leads_followup_expert('{{expert_id}}')`
- For each expert iteration:
  - Get expert's UAZAPI tokens: `SELECT token, instancia, telefone_enviou FROM whatsapp_rotacao WHERE expert_id = '{{expert_id}}' AND ativo = true ORDER BY ordem`
  - Get expert's configs: `SELECT chave, valor FROM configuracoes WHERE expert_id = '{{expert_id}}'`
  - Process leads with this expert's tokens/voice/nome_assistente
- Replace "Helena" with `{{nome_assistente}}`
- Audio generation uses `{{voice_id}}` (skip if empty)

**Workflow 3: Envio Mensagem - Saas (7WKSEmy5qfjb2MKu)**
- Trigger: Webhook (receives expert_id from frontend — enriched by Plan 05-01)
- Add: Validation node after trigger:
  - SQL: `SELECT validate_webhook_expert('{{$json.expert_id}}', '{{$json.instancia}}')`
  - If false: return error and stop
- Add: Expert resolution:
  - SQL: `SELECT nome_assistente, voice_id, voice_settings FROM experts WHERE id = '{{$json.expert_id}}'`
- Modify: All Supabase queries — add `AND expert_id = '{{expert_id}}'`
- Modify: UAZAPI calls — use token from whatsapp_rotacao for this expert
- Modify: Replace "Helena" with `{{nome_assistente}}`
- Modify: Minimax calls — use expert's voice_id

**For each workflow section, include:**
1. Current state (from inspection)
2. Nodes to ADD (with exact parameters, SQL, connections)
3. Nodes to MODIFY (which field, old value → new value)
4. Nodes to REMOVE (if any)
5. Connection changes (which output → which input)
6. Testing steps (how to verify the workflow works)

Write the guide as a structured markdown document that the user can follow step-by-step in the n8n editor.
  </action>
  <verify>
    <automated>test -f ".planning/phases/05-whatsapp-n8n-workflows-voice/N8N_MIGRATION_GUIDE.md" && wc -l ".planning/phases/05-whatsapp-n8n-workflows-voice/N8N_MIGRATION_GUIDE.md" | awk '{print $1}' && echo "Guide exists"</automated>
  </verify>
  <acceptance_criteria>
    - N8N_MIGRATION_GUIDE.md exists with detailed instructions for all 3 workflows
    - Boas vindas section includes expert resolution from instance name
    - Follow up section uses expert LOOP pattern (SplitInBatches over active experts)
    - Envio Mensagem section includes validate_webhook_expert validation
    - All sections address: expert_id filtering, token routing, nome_assistente, voice_id, configuracoes
    - Testing steps included for each workflow
  </acceptance_criteria>
  <done>Comprehensive N8N migration guide generated for all 3 message workflows with step-by-step manual editing instructions.</done>
</task>

</tasks>

<verification>
1. Get workflow details for all 3 workflows and confirm expert_id resolution nodes exist
2. Confirm all Supabase query nodes include expert_id in WHERE clause
3. Confirm UAZAPI HTTP Request nodes use dynamic token from expert resolution
4. Confirm "Helena" string is replaced with nome_assistente reference in all text nodes
5. Confirm Minimax TTS nodes use dynamic voice_id from expert record
6. Confirm Envio Mensagem validates expert_id via RPC before processing
</verification>

<success_criteria>
- 3 workflows (Boas vindas, Follow up, Envio Mensagem) are multi-tenant aware
- Expert_id resolved from instance for trigger-based workflows
- Expert_id validated from payload for frontend-triggered workflows
- All queries, tokens, names, and voice settings are expert-scoped
</success_criteria>

<output>
After completion, create `.planning/phases/05-whatsapp-n8n-workflows-voice/05-02-SUMMARY.md`
</output>
